---
import BaseLayout from '../layouts/BaseLayout.astro';
import ResponsiveEditorialImage from '../components/ResponsiveEditorialImage.astro';
---

<BaseLayout
  title={"Auspicious Music — Viola, Eventmusik und Repertoire"}
  description={"Die neue Viola-Fachseite für Eventmusik, Repertoireplanung und Aufnahme mit Kim-Marie Borger."}
  canonicalPath={"/"}
  lang={"de"}
  noindex={false}
  loadStringField={true}
  headerOverDark={true}
>
<Fragment slot="head">
<link
  rel="preload"
  as="image"
  href="/assets/hero-portrait-760.avif"
  imagesrcset="/assets/hero-portrait-480.avif 480w, /assets/hero-portrait-760.avif 760w, /assets/hero-portrait-928.avif 928w"
  imagesizes="(min-width: 1000px) 53vw, 100vw"
  type="image/avif"
  fetchpriority="high"
/>
<style is:global>
@layer components {

  /* ══ Hero ═══════════════════════════════════════════════════════════
     Revision 5 — the immersive stage.

     Revision 4 put the argument in typography and made the string
     playable, but it played on the same paper as everything after it,
     at 13% ink: the site's one recognisable gesture was a whisper.

     This pass moves the masthead onto the stage. Three architectures
     were weighed: an editorial split (type left, strings right) separates
     exactly the two things that must read as one instrument; an
     instrument-canvas on paper keeps the flatness that was the problem;
     the immersive stage wins because darkness is what makes a string
     visible — ivory lines under a high centre light, a copper node
     glowing where the pluck landed. The header goes transparent and the
     scene extends underneath it; the next section lays its paper edge
     over the stage floor, so the first viewport already promises a page
     to read. Still the only motion above the fold, still never loops,
     still stops dead at rest.                                          */
  .hero {
    position: relative;
    overflow: clip;
    /* The scene extends under the transparent header. */
    margin-top: calc(-1 * var(--header-h));
    padding-top: var(--header-h);
    /* Short of the full viewport by exactly the strip in which the next
       section's paper edge appears. */
    min-block-size: calc(100svh - 2rem);
    display: flex;
    background-color: var(--stage);
    background-image:
      radial-gradient(110% 85% at 50% -18%, rgba(243, 240, 233, 0.10), transparent 62%),
      radial-gradient(70% 55% at 88% 108%, rgba(43, 79, 215, 0.07), transparent 70%),
      linear-gradient(var(--stage-deep), var(--stage) 26%, var(--stage) 80%, var(--stage-deep));
    color: var(--stage-text);
  }
  .hero::before {
    content: "";
    position: absolute;
    inset: 0;
    background: var(--grain);
    background-size: 160px 160px;
    opacity: 0.05;
    pointer-events: none;
  }
  .hero > .shell {
    position: relative;
    z-index: 1;                /* type sits above the string field */
    inline-size: 100%;
    display: grid;
    grid-template-rows: 1fr auto;
    padding-block: clamp(2rem, 5vh, 4rem) 0;
  }

  /* Reference composition: editorial copy on the left, a working piece of
     the studio floating on the right. The string field still owns the
     whole stage behind both. */
  .hero__grid {
    display: grid;
    gap: clamp(2.5rem, 5vw, 4rem);
    align-items: center;
    align-self: center;
    padding-block: clamp(1rem, 3vh, 2.5rem);
  }
  @media (min-width: 1080px) {
    .hero__grid { grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr); }
  }

  .hero__note {
    max-width: 46ch;
    font-size: 1.0625rem;
    line-height: 1.6;
    color: var(--stage-muted);
    margin-top: 1.5rem;
  }

  .hero__title {
    font-size: clamp(2.75rem, 1.2rem + 5.8vw, 5.5rem);
    line-height: 0.95;
    letter-spacing: -0.042em;
    font-weight: 640;
    color: var(--stage-text);
  }
  .hero__title span { display: block; }

  .hero__actions { margin-top: 2rem; }

  .hero__stats {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.75rem;
    margin-top: clamp(2rem, 4vh, 3rem);
    max-inline-size: 34rem;
  }
  .hero__stats .stat-tile span { white-space: normal; }
  @media (max-width: 520px) { .hero__stats { grid-template-columns: repeat(2, minmax(0, 1fr)); } }

  /* The console: not a screenshot of a product — the product. The same
     arithmetic as the full calculator, floating over the stage the way
     the reference layouts float their dashboards. */
  .hero__console-wrap { position: relative; }
  .hero__console-wrap::before {
    content: "";
    position: absolute;
    inset: -12% -8%;
    background:
      radial-gradient(52% 48% at 30% 32%, rgba(43, 79, 215, 0.32), transparent 68%),
      radial-gradient(46% 44% at 76% 78%, rgba(196, 104, 31, 0.24), transparent 70%);
    filter: blur(6px);
    pointer-events: none;
  }
  .hero__console { max-inline-size: 34rem; margin-inline: auto; }
  @media (min-width: 1080px) and (prefers-reduced-motion: no-preference) {
    .hero__console {
      transform: perspective(1400px) rotateY(-8deg) rotateX(2.5deg);
      transition: transform 700ms var(--ease);
    }
    .hero__console:hover { transform: perspective(1400px) rotateY(-2deg) rotateX(0.5deg); }
  }
  .console-live { padding: clamp(1.25rem, 2.5vw, 1.75rem); display: grid; gap: 1.25rem; }
  .console-live label {
    display: block;
    font-family: var(--font-mono); font-size: var(--t-meta);
    letter-spacing: var(--tr-meta); color: var(--stage-muted);
    margin-bottom: 0.5rem;
  }
  .console-live input {
    inline-size: 100%;
    min-block-size: 52px;
    padding-inline: 1rem;
    border-radius: var(--r-control);
    border: 1px solid var(--stage-line);
    background: var(--stage-high);
    color: var(--stage-text);
    font-size: 1.5rem; font-weight: 615;
    font-variant-numeric: tabular-nums;
    transition: border-color var(--dur-fast) var(--ease);
  }
  .console-live input:hover { border-color: var(--stage-muted); }
  .console-live :focus-visible { outline-color: var(--accent-on-dark); }
  .console-live__grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.625rem;
  }
  .console-live__grid > div {
    border: 1px solid var(--stage-line-soft);
    border-radius: var(--r-sm);
    padding: 0.75rem 0.875rem;
    background: color-mix(in srgb, var(--stage-high) 45%, transparent);
  }
  .console-live__grid dt {
    font-family: var(--font-mono); font-size: var(--t-meta);
    letter-spacing: var(--tr-meta); color: var(--stage-muted);
  }
  .console-live__grid dd {
    font-size: clamp(1.25rem, 1rem + 0.9vw, 1.625rem);
    font-weight: 640; letter-spacing: -0.015em;
    font-variant-numeric: tabular-nums;
    margin-top: 0.125rem;
  }
  .console-live__grid small { font-size: 0.8125rem; font-weight: 500; color: var(--stage-muted); margin-left: 0.2rem; }
  .console-live__wave { display: flex; align-items: flex-end; gap: 3px; block-size: 34px; }
  .console-live__wave i {
    flex: 1;
    border-radius: 2px;
    background: var(--ember-bright);
    opacity: 0.65;
    block-size: calc(18% + var(--h, 30%));
  }
  .console-live__foot {
    display: flex; justify-content: space-between; gap: 1rem; flex-wrap: wrap;
    font-family: var(--font-mono); font-size: var(--t-meta);
    letter-spacing: var(--tr-meta); color: var(--stage-muted);
  }
  .console-live__foot a { color: var(--stage-text); }

  /* Short windows: scale gives way rather than the scene overflowing. */
  @media (min-width: 900px) and (max-height: 760px) {
    .hero__title { font-size: clamp(2.5rem, 1rem + 4.2vw, 4rem); }
    .hero__stats { margin-top: 1.5rem; }
  }

  /* The string field owns the whole stage, header strip included.
     Transparent to clicks so the type, the actions and the selection
     behaviour underneath are untouched; the pointer is read from .hero
     itself. Colour is decided here, not in the script: ivory lines,
     copper node — stage lighting, not document ink. */
  .hero__string {
    position: absolute;
    inset: 0;
    inline-size: 100%;
    block-size: 100%;
    pointer-events: none;
    z-index: 0;
    --string-line: var(--stage-text);
    --string-node: var(--ember-bright);
  }

  .hero__foot {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 1.25rem 2rem;
    /* Content ends above the paper edge that the next section lays over
       the stage floor. */
    padding-bottom: calc(var(--sheet-overlap, 3.5rem) + clamp(1.5rem, 3vh, 2.25rem));
  }
  .hero__actions { display: flex; flex-wrap: wrap; gap: 0.75rem; }
  .hero .btn--quiet { border-color: var(--stage-line); color: var(--stage-text); }
  .hero .btn--quiet:hover { border-color: var(--stage-text); background: var(--stage-raised); }
  .hero :focus-visible { outline-color: var(--accent-on-dark); }

  .hero__hint {
    font-family: var(--font-mono);
    font-size: var(--t-meta);
    letter-spacing: var(--tr-caps);
    text-transform: uppercase;
    color: var(--stage-muted);
    line-height: 1.6;
  }
  .hero__hint button {
    font: inherit;
    letter-spacing: inherit;
    text-transform: inherit;
    color: var(--stage-text);
    border-bottom: 1px solid var(--stage-line);
    padding-bottom: 1px;
    transition: border-color var(--dur-fast) var(--ease);
  }
  .hero__hint button:hover { border-bottom-color: var(--stage-text); }


  .line-mask { display: block; overflow: hidden; padding-bottom: 0.06em; }
  .line-mask > span { display: block; }
  @media (prefers-reduced-motion: no-preference) {
    .line-mask > span { animation: line-in 620ms var(--ease-out) both; }
    .line-mask:nth-of-type(2) > span { animation-delay: 80ms; }
  }
  @keyframes line-in { from { transform: translateY(102%); } }

  /* ══ 2 · topic cards ════════════════════════════════════════════════
     Four lifted cards, each carrying its topic's diagram as a mark —
     the reference language (feature tiles) drawn in the house line
     vocabulary instead of icon fonts. */
  .topic-grid { display: grid; gap: clamp(1rem, 2vw, 1.5rem); }
  @media (min-width: 720px) { .topic-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  .topic-card {
    display: grid;
    gap: 0.625rem;
    padding: clamp(1.5rem, 3vw, 2.25rem);
    align-content: start;
  }
  .topic-card svg { inline-size: 64px; block-size: 44px; color: var(--ink); margin-bottom: 0.5rem; }
  .topic-card h3 { font-size: clamp(1.5rem, 1.2rem + 1vw, 2rem); letter-spacing: -0.019em; }
  .topic-card p { color: var(--muted); font-size: 1rem; line-height: 1.6; max-width: 46ch; }
  .topic-card__go {
    display: inline-flex; align-items: center; gap: 0.4375rem;
    font-size: var(--t-ui-sm); font-weight: 550; margin-top: 0.375rem;
    transition: gap var(--dur-fast) var(--ease);
  }
  .topic-card:hover .topic-card__go { gap: 0.6875rem; color: var(--ember); }
  .topic-card__go svg { inline-size: 14px; block-size: 14px; margin: 0; }

  /* ══ 3 · start here ═════════════════════════════════════════════════ */
  .start { display: grid; grid-template-columns: minmax(0, 1fr); gap: clamp(2.5rem, 5vw, 4rem); }
  @media (min-width: 1000px) { .start { grid-template-columns: minmax(0, 1.15fr) minmax(0, 1fr); gap: 4.5rem; } }
  .start__lead h3 { font-size: clamp(1.625rem, 1.15rem + 1.7vw, 2.375rem); letter-spacing: -0.019em; margin-top: 1.25rem; }
  .start__lead p { color: var(--ink-soft); margin-top: 0.875rem; font-size: 1.0625rem; line-height: 1.6; max-width: 48ch; }
  .start__lead .meta { margin-top: 1.25rem; }
  .start__lead a { text-decoration: none; display: block; }
  .start__lead a:hover h3 { text-decoration: underline; text-underline-offset: 4px; text-decoration-thickness: 1px; }
  .start__lead .ph { aspect-ratio: 16 / 10; background-color: var(--surface); box-shadow: var(--shadow-lift); }

  /* The reading list sits in a lifted panel rather than on bare rules. */
  ol.numbered {
    background: var(--surface-bright);
    border: 1px solid var(--line);
    border-radius: var(--r-lg);
    box-shadow: var(--shadow-lift);
    padding-inline: clamp(1.25rem, 2.5vw, 1.75rem);
    overflow: clip;
  }
  .numbered li + li { border-top: 1px solid var(--line); }
  .numbered a {
    display: grid; grid-template-columns: 2.25rem minmax(0, 1fr);
    gap: 0.375rem 1rem; padding-block: 1.375rem; text-decoration: none;
    transition: background-color var(--dur-fast) var(--ease);
  }
  .numbered a:hover { background: var(--surface); }
  .numbered__n {
    font-family: var(--font-mono); font-size: var(--t-meta);
    color: var(--muted); padding-top: 0.28rem; font-variant-numeric: tabular-nums;
  }
  .numbered h4 { font-size: 1.125rem; letter-spacing: -0.012em; }
  .numbered .meta { grid-column: 2; margin-top: 0.375rem; }

  /* ══ 4 · featured lab ═══════════════════════════════════════════════ */
  .lab__inner { display: grid; gap: clamp(2.5rem, 5vw, 4rem); align-items: center; }
  @media (min-width: 1000px) { .lab__inner { grid-template-columns: minmax(0, 1.25fr) minmax(0, 1fr); } }
  .lab h2 { font-size: clamp(2rem, 1.15rem + 2.6vw, 3.25rem); max-width: 18ch; }
  .lab p { color: var(--dark-muted); margin-top: 1.25rem; max-width: 46ch; font-size: 1.0625rem; line-height: 1.65; }
  .lab .meta { margin-top: 1.75rem; }
  .lab .link { margin-top: 2rem; }
  .lab__art {
    border-radius: var(--r-media); overflow: hidden;
    background: var(--stage-raised);
    border: 1px solid var(--stage-line-soft);
    box-shadow: var(--shadow-float);
  }
  .lab__art img {
    inline-size: 100%;
    aspect-ratio: 5 / 4;
    object-fit: cover;
    object-position: 55% 62%;
  }

  /* ══ 5 · tools ══════════════════════════════════════════════════════
     Three small applications, presented as applications: each card opens
     with a dark readout head showing a real computed value, labelled with
     the exact input it was computed from. Nothing is a mock number.    */
  .tools-band { background: var(--tint); }
  .tools-grid { display: grid; gap: clamp(1rem, 2vw, 1.5rem); }
  @media (min-width: 860px) { .tools-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); } }
  .tool-app { overflow: clip; display: grid; grid-template-rows: auto 1fr; }
  .tool-app__head {
    background: linear-gradient(178deg, var(--stage-raised), var(--stage) 70%);
    color: var(--stage-text);
    padding: 1.25rem 1.375rem 1.375rem;
    display: grid;
    gap: 0.25rem;
  }
  .tool-app__head .app-frame__title { margin-bottom: 0.625rem; }
  .tool-app__value {
    font-size: clamp(1.875rem, 1.4rem + 1.6vw, 2.5rem);
    font-weight: 640; letter-spacing: -0.02em;
    font-variant-numeric: tabular-nums;
  }
  .tool-app__value small { font-size: 1rem; font-weight: 500; color: var(--stage-muted); margin-left: 0.25rem; }
  .tool-app__ctx {
    font-family: var(--font-mono); font-size: var(--t-meta);
    letter-spacing: var(--tr-meta); color: var(--stage-muted);
  }
  .tool-app__body { padding: 1.25rem 1.375rem 1.5rem; display: grid; gap: 0.5rem; align-content: start; }
  .tool-app__body h3 { font-size: 1.25rem; letter-spacing: -0.014em; }
  .tool-app__body p { color: var(--muted); font-size: 0.9875rem; line-height: 1.55; }
  .tool-app__io {
    font-family: var(--font-mono); font-size: var(--t-meta);
    letter-spacing: var(--tr-meta); color: var(--ink-soft);
    margin-top: 0.375rem;
  }
  @media (min-width: 900px) {
    .tool-row a { grid-template-columns: minmax(0, 16rem) minmax(0, 1fr) minmax(0, 11rem); align-items: start; }
    .tool-row__io { text-align: right; padding-top: 0.3rem; }
  }

  /* ══ 6 · archive ════════════════════════════════════════════════════ */
  .records-grid { display: grid; gap: clamp(1rem, 2vw, 1.5rem); align-items: start; }
  @media (min-width: 860px) { .records-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  .record { padding: clamp(1.5rem, 3vw, 2.25rem); }
  .record__grid { display: grid; gap: 1.25rem; }
  .record h3 { font-size: clamp(1.5rem, 1.15rem + 1.1vw, 2rem); letter-spacing: -0.017em; margin-top: 0.625rem; }
  .record p { color: var(--ink-soft); font-size: 1.0625rem; line-height: 1.65; max-width: 60ch; }
  .record__facts { display: grid; gap: 0.625rem; margin-top: 1.5rem; }
  .record .link { margin-top: 1.5rem; }
  .fact {
    display: grid; grid-template-columns: 8.5rem minmax(0, 1fr); gap: 0.75rem;
    font-family: var(--font-mono); font-size: var(--t-meta);
    letter-spacing: var(--tr-meta); padding-block: 0.5rem;
    border-top: 1px solid var(--line);
  }
  .fact dt { color: var(--muted); }
  .fact dd { color: var(--ink-soft); }
  @media (max-width: 560px) { .fact { grid-template-columns: 1fr; gap: 0.1875rem; } }

  /* ══ 7 · acoustic story ═════════════════════════════════════════════ */
  .story__media {
    position: relative; border-radius: var(--r-media); overflow: hidden;
    box-shadow: var(--shadow-media);
  }
  .story__media img { aspect-ratio: 16 / 9; object-fit: cover; }
  .story__body { display: grid; gap: clamp(1.5rem, 3vw, 2.5rem); margin-top: clamp(2rem, 4vw, 3rem); }
  @media (min-width: 900px) { .story__body { grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); gap: 4rem; } }
  .story h2 { max-width: 16ch; }
  .story p { color: var(--ink-soft); font-size: 1.0625rem; line-height: 1.7; max-width: 56ch; }
  .story__links { display: flex; flex-wrap: wrap; gap: 0.75rem 1.5rem; margin-top: 1.75rem; }

  /* ══ 8 · latest — the index in a lifted panel ═══════════════════════ */
  .index--panel {
    background: var(--surface-bright);
    border: 1px solid var(--line);
    border-radius: var(--r-lg);
    box-shadow: var(--shadow-lift);
    padding-inline: clamp(1.25rem, 2.5vw, 1.75rem);
    overflow: clip;
  }
  .index--panel li:first-child a { border-top: none; }
}
</style>
</Fragment>

<main id="content">

  <!-- 1 ─────────────────────────────────────────────────────── hero -->
  <section class="hero" data-od-id="hero" data-stage-scene aria-labelledby="hero-title">
    <canvas class="hero__string" data-string aria-hidden="true"></canvas>

    <div class="shell">

      <div class="hero__grid">
        <div>
          <h1 id="hero-title" class="hero__title">
            <span>Viola, Anlässe</span>
            <span>und die Entscheidungen dazwischen.</span>
          </h1>
          <p class="hero__note">
            Auspicious Music ist eine neue Viola-Fachseite mit Kim-Marie Borger — für konkrete Eventablaeufe, Repertoireentscheidungen und Aufnahmepraxis, nicht als Fortsetzung des früheren Katalogs.
          </p>
          <div class="hero__actions">
            <a class="btn btn--primary" href="/tools/eventmusik-planer/">Eventmusik planen</a>
            <a class="btn btn--quiet" href="/viola/">Viola-Fachseite entdecken</a>
          </div>
          <ul class="hero__stats">
            <li class="stat-tile"><strong>Viola</strong><span>benannte aktuelle Expertise</span></li>
            <li class="stat-tile"><strong>Local</strong><span>Planerdaten bleiben im Browser</span></li>
            <li class="stat-tile"><strong>Neu</strong><span>klar getrennte Eigentümerschaft und Arbeit</span></li>
          </ul>
        </div>

        <div class="hero__console-wrap">
          <div class="app-frame hero__console">
            <div class="app-frame__bar">
              <span class="app-frame__dots" aria-hidden="true"><i></i><i></i><i></i></span>
              <span class="app-frame__title">eventmusik-plan — lokales Tool</span>
            </div>
            <div class="console-live">
              <dl class="console-live__grid">
                <div><dt>01 · Anlass</dt><dd>Rahmen setzen</dd></div>
                <div><dt>02 · Momente</dt><dd>Signale planen</dd></div>
                <div><dt>03 · Richtung</dt><dd>Klang benennen</dd></div>
                <div><dt>04 · Praxis</dt><dd>Grenzen festhalten</dd></div>
              </dl>
              <div class="console-live__wave" aria-hidden="true">
                <i style="--h:22%"></i><i style="--h:58%"></i><i style="--h:36%"></i><i style="--h:74%"></i><i style="--h:48%"></i><i style="--h:82%"></i><i style="--h:30%"></i><i style="--h:64%"></i><i style="--h:42%"></i><i style="--h:70%"></i><i style="--h:26%"></i><i style="--h:54%"></i>
              </div>
              <p class="console-live__foot"><span>Kein Konto · kein Upload</span><a href="/tools/eventmusik-planer/">Planer öffnen →</a></p>
            </div>
          </div>
        </div>
      </div>

      <div class="hero__foot">
        <p class="hero__hint">
          <button type="button" data-string-pluck>Saiten anspielen</button> — zieh eine an, die anderen reagieren.
        </p>
      </div>
    </div>
  </section>

  <!-- 2 · topic cards — the paper edge over the stage floor -->
  <section class="band band--tight sheet-lift band--glow" aria-labelledby="portals-title">
    <div class="shell" style="position:relative">
      <span class="rail" data-rail style="left:calc(var(--gutter) * -0.55)" aria-hidden="true"></span>
      <h2 id="portals-title" style="position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%)">Einstieg nach Thema</h2>
      <div class="topic-grid" data-reveal-group>
        <a class="card topic-card" href="/viola/" data-reveal><svg viewBox="0 0 64 44" fill="none" aria-hidden="true"><path d="M14 41C6 41 5 32 10 28C6 24 8 17 13 16C9 11 13 4 19 5C25 6 27 12 23 17C29 20 28 26 25 29C29 33 26 41 19 41Z" stroke="currentColor"/><path d="M42 3v36M48 3v36" style="stroke:var(--ember-bright)"/><path d="M35 9h20M35 35h20" stroke="currentColor" opacity=".35"/></svg><h3>Viola</h3><p>Die Viola über ihre konkrete Aufgabe bei Anlass, Arrangement oder Aufnahme verstehen — mit Kim-Marie Borger als benannter Expertin.</p><span class="topic-card__go">Bereich öffnen →</span></a>
        <a class="card topic-card" href="/eventmusik/" data-reveal><svg viewBox="0 0 64 44" fill="none" aria-hidden="true"><path d="M2 22h60M2 17v10M22 17v10M42 17v10M62 17v10" stroke="currentColor" opacity=".5"/><circle cx="22" cy="22" r="4" style="fill:var(--ember-bright)"/><circle cx="42" cy="22" r="4" fill="currentColor" opacity=".6"/></svg><h3>Eventmusik</h3><p>Spielmomente, Signale, flexible Laengen und Raumbedingungen planen, bevor Titel oder Angebot feststehen.</p><span class="topic-card__go">Bereich öffnen →</span></a>
        <a class="card topic-card" href="/repertoire/" data-reveal><svg viewBox="0 0 64 44" fill="none" aria-hidden="true"><path d="M4 10h56M4 16h56M4 22h56M4 28h56M4 34h56" stroke="currentColor" opacity=".3"/><path d="M22 7v22c0 7-12 9-12 2 0-5 8-8 12-4M44 15v22c0 7-12 9-12 2 0-5 8-8 12-4" style="stroke:var(--ember-bright)"/></svg><h3>Repertoire</h3><p>Nach Funktion, Tonumfang, Arrangement-Aufwand und Rechten wählen, statt einen Titel als automatische Zusage zu behandeln.</p><span class="topic-card__go">Bereich öffnen →</span></a>
        <a class="card topic-card" href="/recording/" data-reveal><svg viewBox="0 0 64 44" fill="none" aria-hidden="true"><path d="M10 40C4 40 3 32 7 29C3 26 4 18 9 17C6 13 8 7 13 7C18 7 20 13 17 17C22 18 23 26 19 29C23 32 22 40 16 40Z" stroke="currentColor" opacity=".55"/><path d="M30 36A18 18 0 0 0 30 8M46 42A30 30 0 0 0 46 2" stroke="currentColor" opacity=".3" stroke-dasharray="2 4"/><circle cx="33" cy="16" r="3.5" style="fill:var(--ember-bright)"/></svg><h3>Recording</h3><p>Nachvollziehbare Raum- und Mikrofonentscheidungen für Viola und akustische Streicher, mit sichtbaren Variablen und Testgrenzen.</p><span class="topic-card__go">Bereich öffnen →</span></a>
      </div>
    </div>
  </section>

  <!-- 3 · start here -->
  <section class="band band--glow" aria-labelledby="start-title">
    <div class="shell">
      <div class="staff" style="--staff-node-x:26%;margin-bottom:clamp(2rem,4vw,3rem)" aria-hidden="true"></div>
      <div class="section-head"><h2 id="start-title">Mit dem konkreten Anlass beginnen</h2><p>Beginne mit dem neuen Planer und nutze die Guides dort, wo eine musikalische oder technische Entscheidung mehr Tiefe braucht.</p></div>
      <div class="start">
        <article class="start__lead" data-reveal>
          <a href="/tools/eventmusik-planer/">
            <div class="ph" aria-hidden="true"><span class="ph__label"><strong>Eventmusik-Plan</strong><br>Anlass · Momente · Signale · Repertoire · praktische Grenzen</span></div>
            <h3>Erst den Ablauf, dann die Stücke</h3>
            <p>Verbinde Ablauf und Raum zu einer brauchbaren ersten Anfrage, ohne Daten an einen Server zu senden.</p>
            <p class="meta"><span>Eventmusik</span><span>Lokales Browser-Tool</span><span>Kein Konto</span></p>
          </a>
        </article>
        <ol class="numbered" data-reveal-group>
          <li data-reveal><a href="/recording/acoustic-instruments/viola-microphone-placement/"><span class="numbered__n">01</span><h4>Viola microphone placement: three useful starting positions</h4><p class="meta"><span>Recording</span><span>9 min</span></p></a></li>
          <li data-reveal><a href="/sound-design/when-to-record-strings-instead-of-using-a-library/"><span class="numbered__n">02</span><h4>When to record a string part instead of using a library</h4><p class="meta"><span>Sound Design</span><span>8 min</span></p></a></li>
          <li data-reveal><a href="/repertoire/"><span class="numbered__n">03</span><h4>Repertoire nach Funktion und Machbarkeit wählen</h4><p class="meta"><span>Repertoire</span><span>Decision hub</span></p></a></li>
          <li data-reveal><a href="/music-for-media/planning-live-viola-for-a-wedding-ceremony/"><span class="numbered__n">04</span><h4>Planning live viola for a wedding ceremony</h4><p class="meta"><span>Events</span><span>7 min</span></p></a></li>
          <li data-reveal><a href="/recording/acoustic-instruments/recording-viola-at-home/"><span class="numbered__n">05</span><h4>A repeatable plan for recording viola at home</h4><p class="meta"><span>Recording</span><span>9 min</span></p></a></li>
        </ol>
      </div>
    </div>
  </section>

  <!-- 4 · featured method -->
  <section class="band band--wide band-dark lab" aria-labelledby="lab-title">
    <div class="shell lab__inner">
      <figure class="lab__art" data-reveal><ResponsiveEditorialImage asset="hero-portrait" priority={true} sizes="(min-width: 1000px) 53vw, 100vw" alt="Abstract editorial image of a metal block and a tensioned string." /><figcaption class="kicker" style="padding:1rem">Editorial illustration — not a test photograph</figcaption></figure>
      <div data-reveal data-reveal-delay="120"><p class="kicker">Recording method</p><h2 id="lab-title" style="margin-top:.875rem">Record viola so tomorrow’s comparison is still fair</h2><p>Mark distance, angle, height and room position; record level-matched takes; keep the performance decision separate from processing. The result is a method another session can repeat.</p><p class="meta"><span>Viola</span><span>Room-aware workflow</span><span>9 min</span></p><a class="link" href="/recording/acoustic-instruments/recording-viola-at-home/">Read the recording plan →</a></div>
    </div>
  </section>

  <!-- 5 · tools -->
  <section class="band tools-band" aria-labelledby="tools-title">
    <div class="shell">
      <div class="section-head"><h2 id="tools-title">Kleine Tools, konkrete Antworten</h2><p>Eventmusik-Planer und Musikrechner laufen ohne Konto oder Upload direkt im Browser.</p></div>
      <div class="tools-grid" data-reveal-group>
        <a class="card tool-app" href="/tools/eventmusik-planer/" data-reveal>
          <div class="tool-app__head">
            <span class="app-frame__title">ceremony-brief</span>
            <span class="tool-app__value">4<small>decision steps</small></span>
            <span class="tool-app__ctx">Occasion · moments · sound · limits</span>
          </div>
          <div class="tool-app__body">
            <h3>Eventmusik-Planer</h3>
            <p>Erstelle einen kopierbaren oder herunterladbaren Plan, ohne persönliche Daten an einen Server zu senden.</p>
            <span class="tool-app__io">Entscheidungen → brauchbarer Plan</span>
          </div>
        </a>
        <a class="card tool-app" href="/resources/tools/note-frequency/" data-reveal>
          <div class="tool-app__head">
            <span class="app-frame__title">note-frequency</span>
            <span class="tool-app__value">440.00<small>Hz</small></span>
            <span class="tool-app__ctx">A4 at concert pitch 440</span>
          </div>
          <div class="tool-app__body">
            <h3>Note &amp; Frequency</h3>
            <p>Move between note names, MIDI numbers, frequencies and cents — plus wavelength for room problems.</p>
            <span class="tool-app__io">Note ⇄ Hz ⇄ MIDI</span>
          </div>
        </a>
        <a class="card tool-app" href="/resources/tools/sample-pitch/" data-reveal>
          <div class="tool-app__head">
            <span class="app-frame__title">sample-pitch</span>
            <span class="tool-app__value">0.500<small>× rate</small></span>
            <span class="tool-app__ctx">−12 semitones, resampled</span>
          </div>
          <div class="tool-app__body">
            <h3>Sample Pitch &amp; Rate</h3>
            <p>See how transposition changes playback rate, duration and loop tempo before you commit.</p>
            <span class="tool-app__io">Semitones → rate · length</span>
          </div>
        </a>
      </div>
      <p style="margin-top:2rem"><a class="link" href="/resources/tools/">All tools →</a></p>
    </div>
  </section>

  <!-- 6 · archive -->
  <section class="band" aria-labelledby="legacy-title">
    <div class="shell">
      <div class="section-head"><h2 id="legacy-title">Das Archiv ist Kontext, keine geliehene Identität</h2><p>Alte Adressen bleiben dort hilfreich, wo sich ihre frühere Absicht belegen lässt. Dateien und Urheberschaft des früheren Betreibers werden nicht beansprucht.</p></div>
      <div class="records-grid" data-reveal-group>
        <article class="card record" data-reveal><div class="record__grid"><div><span class="flag">Historical resource</span><h3>The Spy Guitar</h3></div><div><p>A documented former Ableton sampler instrument. The current page explains what can be verified and why no unlicensed file is redistributed.</p><dl class="record__facts"><div class="fact"><dt>Rights</dt><dd>Not transferred with the domain</dd></div><div class="fact"><dt>Status</dt><dd><span class="status" data-state="unavailable">Documentation only</span></dd></div></dl><a class="link" href="/legacy/the-spy-guitar/">Read the archive record →</a></div></div></article>
        <article class="card record" data-reveal><div class="record__grid"><div><span class="flag">Historical resource</span><h3>Bang My Twister</h3></div><div><p>A former Max for Live/Twister project documented without claiming the previous operator’s work. External records are linked at their source.</p><dl class="record__facts"><div class="fact"><dt>Rights</dt><dd>Remain with their respective owners</dd></div><div class="fact"><dt>Status</dt><dd><span class="status" data-state="external">External records</span></dd></div></dl><a class="link" href="/legacy/bang-my-twister/">Read the archive record →</a></div></div></article>
      </div>
    </div>
  </section>

  <!-- 7 · strings and applications -->
  <section class="band band--wide story" aria-labelledby="story-title">
    <div class="shell">
      <figure class="story__media" data-reveal><ResponsiveEditorialImage asset="story-viola-back" sizes="(min-width: 1440px) 1376px, 100vw" alt="Editorial illustration of a viola and microphone in a quiet room." /><figcaption class="kicker" style="margin-top:.75rem">Editorial illustration — not a documented session</figcaption></figure>
      <div class="story__body"><h2 id="story-title">Ein Instrument, mehrere konkrete Aufgaben</h2><div><p>Eine Viola kann einen persönlichen Moment tragen, als bewusster Programmpunkt auftreten oder eine exponierte Aufnahmespur sein. Mit der Aufgabe aendern sich Ablauf, Repertoire, Mikrofon, Rechte und Vorbereitung. Diese Seite macht die Entscheidungen sichtbar; Kim-Maries eigene Website bleibt die Quelle für aktuelle Auftritte und Buchungen.</p><div class="story__links"><a class="link" href="/viola/">Viola →</a><a class="link" href="/eventmusik/">Anlässe →</a><a class="link" href="/repertoire/">Repertoire →</a><a class="link" href="/recording/">Aufnahme →</a></div></div></div>
    </div>
  </section>

  <!-- 8 · latest -->
  <section class="band band-surface band--glow" aria-labelledby="latest-title">
    <div class="shell">
      <div class="section-head"><h2 id="latest-title">Neu für den Relaunch</h2><p><a class="link" href="/journal/">Journal öffnen →</a></p></div>
      <ul class="index index--panel" data-reveal-group>
        <li data-reveal><a href="/tools/eventmusik-planer/"><span class="index__date">Neu</span><div><h3>Eventmusik-Planer für Viola</h3><p>Ein lokales Tool für Ablauf, Signale, Repertoire und praktische Bedingungen.</p></div><div class="index__tail"><span class="index__topic">Tool · local</span></div></a></li>
        <li data-reveal><a href="/recording/acoustic-instruments/viola-microphone-placement/"><span class="index__date">Guide</span><div><h3>Viola microphone placement: three useful starting positions</h3><p>Compare distance and angle before trying to repair the room with EQ.</p></div><div class="index__tail"><span class="index__topic">Recording · 9 min</span></div></a></li>
        <li data-reveal><a href="/music-for-media/planning-live-viola-for-a-wedding-ceremony/"><span class="index__date">Guide</span><div><h3>Planning live viola for a wedding ceremony</h3><p>Musical moments, timing ranges, cue ownership and practical conditions.</p></div><div class="index__tail"><span class="index__topic">Events · 7 min</span></div></a></li>
        <li data-reveal><a href="/sound-design/when-to-record-strings-instead-of-using-a-library/"><span class="index__date">Guide</span><div><h3>When to record strings instead of using a library</h3><p>Choose live, sampled or hybrid by musical function.</p></div><div class="index__tail"><span class="index__topic">Sound Design · 8 min</span></div></a></li>
      </ul>
    </div>
  </section>

  <!-- 9 · relaunch -->
  <section><div class="shell relaunch"><p>Auspicious Music ist eine neue unabhängige Viola-Fachseite mit Kim-Marie Borger als aktueller Expertin. Der frühere Betreiber und Katalog sind nicht beteiligt.</p><a class="link" href="/about/history-and-relaunch/">So werden Archiv und Neustart getrennt →</a></div></section>

</main>
</BaseLayout>
